import Prelude hiding (sum, product)

-- data List a = Nil a | Cons a (List a) | ...

exercise = error "todo"

sum :: [Int] -> Int
sum xs = exercise

product xs = exercise

append :: ([Int],[Int]) -> [Int]
append (xs,ys) = exercise



